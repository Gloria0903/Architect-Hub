<#
  Architect Hub -- patch 39: adds *.zip to .gitignore so troubleshooting
  artifacts like next_build.zip/standalone_fix.zip can never accidentally
  get committed again.
  Generated by Claude -- 2026-08-28
#>

$ErrorActionPreference = "Stop"
$utf8NoBom = New-Object System.Text.UTF8Encoding($false)

$content = @'
# Dependencies
node_modules/

# Next.js
.next/

# Build output
out/

# Environment
.env
.env.local
.env.development.local
.env.production.local

# Logs
*.log

# VS Code
.vscode/

# OS
.DS_Store
Thumbs.db

# Uploaded files — never belong in git. Local-disk storage is a dev-only
# fallback; production should be using S3 (see src/lib/s3.ts) once
# AWS_S3_BUCKET is configured.
/uploads/
tsconfig.tsbuildinfo

# Zip archives — these should never be committed. Two large ones
# (next_build.zip, standalone_fix.zip) made from earlier troubleshooting
# once slipped past `git add .` and got rejected by GitHub's 100MB file
# size limit -- this rule stops that from ever happening again.
*.zip
'@
[System.IO.File]::WriteAllText((Join-Path (Get-Location) ".gitignore"), $content, $utf8NoBom)
Write-Host "wrote .gitignore" -ForegroundColor Green
