<#
  Architect Hub -- patch 29: fixes the build break from patch 28.
  Generated by Claude -- 2026-08-22

  ROOT CAUSE: topbar.tsx (src/components/layout/topbar.tsx) referenced
  NotificationBell, which patch 28 deleted as dead code. But topbar.tsx
  itself is ALSO dead code -- confirmed nothing anywhere imports or
  renders it. It's legacy scaffold (a hardcoded "LM" placeholder avatar,
  a non-functional search box) that predates the real header system in
  AppShell/ProfileMenu/Sidebar. It only surfaced because TypeScript
  still type-checks unused files during build.

  FIX: deleted topbar.tsx entirely, same reasoning as deleting the dead
  notification-bell.tsx last time -- rather than patch a component
  nothing uses.

  ALSO FOUND while sweeping for anything else like this (flagging, not
  touching -- these aren't breaking your build, just unused):
    - src/components/ui/progress.tsx (Progress)
    - src/components/ui/popover.tsx (Popover/PopoverTrigger/PopoverContent)
    - src/components/documents/document-list.tsx (ProjectDocuments)
    - src/components/ui/dimension-bar.tsx (DimensionBar)
  All four confirmed unused by searching for their actual exported
  component names being rendered anywhere, not just import paths. Let
  me know if you want these removed too, or if any were meant to be
  wired in somewhere.

  62/62 tests on your machine (same 3 sandbox-only failures here,
  confirmed unrelated in every earlier patch).

  Run from the ROOT of your local Architect-Hub repo:
    .\apply-patch-29.ps1
#>

$ErrorActionPreference = "Stop"

Write-Host "Applying Architect Hub patch 29 (remove dead topbar.tsx)..." -ForegroundColor Cyan
Remove-Item -Path "src/components/layout/topbar.tsx" -ErrorAction SilentlyContinue
Write-Host "  removed src/components/layout/topbar.tsx"

Write-Host ""
Write-Host "Patch 29 applied. Run:" -ForegroundColor Green
Write-Host "  npm run build"
